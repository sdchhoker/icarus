const deleteAndEarn = require('./deleteAndEarn');

test("simple case 1", () => {
  let nums = [2, 2, 3, 3, 3, 4];
  const output = 9;
  expect(deleteAndEarn(nums)).toBe(output);
});

test("simple case 2", () => {
  let nums = [2, 2, 3, 3, 3, 4];
  const output = 9;
  expect(deleteAndEarn(nums)).toBe(output);
});